<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shiny="http://shiny.net/maui/controls"
             xmlns:local="clr-namespace:Sample.Features.Scheduler"
             x:Class="Sample.Features.Scheduler.AgendaCalendarPickerPage"
             x:DataType="local:AgendaCalendarPickerViewModel"
             Title="Calendar Picker">

    <Grid RowDefinitions="Auto,*">
        <HorizontalStackLayout HorizontalOptions="End" Spacing="8" Margin="8">
            <Button Text="{Binding DaysToggleText}"
                    Command="{Binding ToggleDaysCommand}"
                    BackgroundColor="DodgerBlue"
                    TextColor="White"
                    CornerRadius="8"
                    FontSize="12" />
        </HorizontalStackLayout>

        <shiny:SchedulerAgendaView Grid.Row="1"
            Provider="{Binding Provider}"
            SelectedDate="{Binding SelectedDate}"
            DaysToShow="{Binding DaysToShow}"
            DatePickerMode="Calendar"
            ShowCarouselDatePicker="True"
            ShowCurrentTimeMarker="True"
            CurrentTimeMarkerColor="Red"
            DefaultEventColor="CornflowerBlue"
            TimeSlotHeight="60"
            Use24HourTime="False" />
    </Grid>

</ContentPage>
