<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shiny="http://shiny.net/maui/controls"
             xmlns:local="clr-namespace:Sample.Features.Fab"
             x:Class="Sample.Features.Fab.FabPage"
             x:DataType="local:FabViewModel"
             Title="FAB &amp; FabMenu">

    <Grid>

        <!-- Main page content -->
        <ScrollView>
            <VerticalStackLayout Padding="24" Spacing="20">

                <Label Text="Floating Action Button"
                       FontSize="22"
                       FontAttributes="Bold" />

                <Label Text="{Binding StatusMessage}"
                       FontSize="14"
                       TextColor="Gray" />

                <BoxView HeightRequest="1" Color="LightGray" />

                <Label Text="Inline Fab (icon only)" FontAttributes="Bold" />
                <shiny:Fab Icon="dotnet_bot.png"
                           IconSize="32"
                           Size="56"
                           FabBackgroundColor="#2196F3"
                           Command="{Binding AddCommand}"
                           HorizontalOptions="Start" />

                <Label Text="Extended Fab (icon + text)" FontAttributes="Bold" />
                <shiny:Fab Icon="dotnet_bot.png"
                           Text="Add Item"
                           FabBackgroundColor="#4CAF50"
                           TextColor="White"
                           Command="{Binding AddCommand}"
                           HorizontalOptions="Start" />

                <Label Text="Outlined Fab (icon + text)" FontAttributes="Bold" />
                <shiny:Fab Icon="dotnet_bot.png"
                           Text="Outlined"
                           FabBackgroundColor="Transparent"
                           BorderColor="#9C27B0"
                           BorderThickness="2"
                           TextColor="#9C27B0"
                           Command="{Binding AddCommand}"
                           HorizontalOptions="Start" />

                <BoxView HeightRequest="1" Color="LightGray" />

                <Label Text="The floating Fab &amp; FabMenu are pinned to the bottom-right. Tap the main FAB to open the menu."
                       FontSize="14"
                       TextColor="Gray"
                       Margin="0,0,0,140" />

            </VerticalStackLayout>
        </ScrollView>

        <!-- Floating single Fab, bottom-left -->
        <shiny:Fab Icon="dotnet_bot.png"
                   FabBackgroundColor="#E91E63"
                   Command="{Binding AddCommand}"
                   HorizontalOptions="Start"
                   VerticalOptions="End"
                   Margin="24" />

        <!-- Floating Fab menu, bottom-right -->
        <shiny:FabMenu IsOpen="{Binding IsMenuOpen}"
                       Icon="dotnet_bot.png"
                       FabBackgroundColor="#2196F3"
                       Margin="0,0,24,24">
            <shiny:FabMenuItem Icon="dotnet_bot.png"
                               Text="Share"
                               FabBackgroundColor="#4CAF50"
                               Command="{Binding ShareCommand}" />
            <shiny:FabMenuItem Icon="dotnet_bot.png"
                               Text="Edit"
                               FabBackgroundColor="#FF9800"
                               Command="{Binding EditCommand}" />
            <shiny:FabMenuItem Icon="dotnet_bot.png"
                               Text="Delete"
                               FabBackgroundColor="#F44336"
                               Command="{Binding DeleteCommand}" />
        </shiny:FabMenu>

    </Grid>
</ContentPage>
